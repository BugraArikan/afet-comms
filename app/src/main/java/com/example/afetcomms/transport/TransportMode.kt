package com.example.afetcomms.transport

enum class TransportMode {
    /** Yalnızca kendi aile koduna ait mesajları alır. */
    FAMILY,
    /** Tüm yakındaki SOS / yardım çağrılarını alır (kurtarıcı). */
    RESCUER
}
